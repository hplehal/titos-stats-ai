# Tito's Courts — repo conventions

See `PROJECT_BRIEF.md` for goals and architecture, `RUNBOOK.md` for the session-by-session plan, and `HOTKEYS.md` for the tracker key map.

## Project conventions

- Test fixtures must reference roster by index or jersey number, never by name — names depend on seed data which can change.
- For tagging semantics (action/result definitions, edge cases), see TAGGING_RULES.md — single source of truth for ground-truth data.
